Title: Add GenerationMixin directly to HybridModel and GLAswaForCausalLM

Summary:
- Both HybridModel and GLAswaForCausalLM now directly inherit GenerationMixin, satisfying Transformers' generate check and removing repeated warnings.

Testing:
- Pending: rerun conversion to verify the warning no longer appears.
