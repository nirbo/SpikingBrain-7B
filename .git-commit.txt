Title: Expand tokenizer to match Qwen3 vocab before resizing embeddings

Summary:
- When loading the tokenizer, add placeholder tokens so its length matches source_config.vocab_size (151936) before copying weights.
- Ensures resize_token_embeddings aligns with checkpoint rows and avoids lm_head/embed mismatch warnings.

Testing:
- Pending: rerun conversion to confirm warnings gone.
