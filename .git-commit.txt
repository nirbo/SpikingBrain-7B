Title: Add scheduler and gradient clipping options

Summary:
- Added CLI arguments --warmup-steps and --max-grad-norm, wiring them into UnslothTrainingArguments so learning rate warmup and gradient clipping can be configured.

Testing:
- Pending: rerun fine-tuning with warmup/clipping flags to check for stability improvements.
