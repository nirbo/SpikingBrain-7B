Title: Prevent prepare_model_for_kbit_training from casting to float32

Summary:
- Call prepare_model_for_kbit_training with cast_input_dtype=False and layer_norm names empty so embeddings remain in the compute dtype for flash attention.
- After attaching LoRA adapters, ensure input embeddings and lm_head are explicitly cast back to the training dtype (fp16/bf16).

Testing:
- Pending: rerun QLoRA command to confirm FlashAttention no longer receives float32 tensors.
