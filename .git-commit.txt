Title: Align vocab resize with tokenizer length and reorder GenerationMixin

Summary:
- After loading Qwen3 tokenizer, set GLAswaConfig.vocab_size=len(tokenizer) and resize token embeddings with the same value to ensure lm_head/embed weights match the checkpoint rows.
- Inherit GenerationMixin ahead of PreTrainedModel so Transformers no longer warns about generate support.

Testing:
- Pending: rerun CPU conversion to confirm warnings are resolved.
