Title: Add optional 4-bit QLoRA path to conversion script

Summary:
- Introduced CLI flags for 4-bit loading (--load-in-4bit) and LoRA settings (rank, alpha, dropout, RSLoRA) to enable memory-efficient fine-tuning.
- When --load-in-4bit is set, use BitsAndBytesConfig to quantize the converted model; optionally wrap with PEFT LoRA adapters targeting attention and MLP projections.
- Gradient checkpointing and cache settings updated to work with both full fine-tuning and QLoRA configurations.

Testing:
- Pending: launch fine-tuning with --load-in-4bit --use-lora to verify reduced VRAM usage.
