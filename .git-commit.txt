Title: Make 4-bit QLoRA training compatible with precision checks

Summary:
- Added --per-device-eval-batch-size and passed it to UnslothTrainingArguments to control eval memory.
- Detected model dtype after loading; if bf16 requested but model remains fp16, automatically fall back to fp16 and warn.
- Forward bf16/fp16 flags explicitly and reuse them in UnslothTrainingArguments.

Testing:
- Pending: rerun 4-bit QLoRA command with bf16 to confirm the precision mismatch error is resolved.
