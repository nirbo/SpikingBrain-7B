Title: Enforce max_seq_length by filtering long conversations

Summary:
- After mapping conversations to text, tokenize with truncation and drop any sample whose length exceeds --max-seq-length before passing to the data collator.

Testing:
- Pending: rerun fine-tuning to confirm step time and loss behave with capped sequence length.
