Title: Keep bf16 requests when using 4-bit QLoRA

Summary:
- Removed dtype fallback logic so bf16 requests are honored even if the quantized base reports float16; compute dtype is already set via BitsAndBytesConfig.
- fp16/bf16 flags now align directly with CLI arguments.

Testing:
- Pending: rerun QLoRA command with --bf16 to confirm FlashAttention uses bf16 without errors.
