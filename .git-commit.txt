Title: Inherit GenerationMixin to silence generate warning

Summary:
- HybridPreTrainedModel now inherits from (PreTrainedModel, GenerationMixin) so GLAswaForCausalLM keeps full generation support without warnings in upcoming Transformers releases.

Testing:
- Pending: rerun conversion to confirm the warning is gone.
