Title: Add Unsloth conversion script and finalize Qwen3 alignment

Summary:
- Fix GLAswaConfig defaults (vocab, FFN, context, EOS) to mirror Qwen3-14B without duplicate arguments.
- Ignore local checkpoint/cache directories in .gitignore.
- Introduce `run_model/convert_qwen3_unsloth.py` to copy Qwen3 weights into the hybrid SpikingBrain model and optionally run Unsloth fine-tuning with gradient checkpointing/accumulation.

Testing:
- Pending: execute conversion script against ./qwen3-14b-ckpt and launch Unsloth fine-tuning once resources are available.
