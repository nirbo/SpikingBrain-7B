Title: Resize token embeddings before copying Qwen3 weights

Summary:
- After instantiating the hybrid model, call resize_token_embeddings(tokenizer.vocab_size) so embedding and lm_head shapes match Qwen3 (151936 slots), eliminating skip warnings during conversion.

Testing:
- Pending: rerun conversion to confirm no shape mismatch warnings for lm_head/embed_tokens.
