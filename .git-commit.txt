Title: Use new prepare_model_for_kbit_training signature

Summary:
- Updated QLoRA flow to call prepare_model_for_kbit_training with only the supported arguments (use_gradient_checkpointing=True).
- Removes unsupported keyword layer_norm_names that caused runtime TypeError.

Testing:
- Pending: rerun 4-bit QLoRA command to confirm it proceeds past model preparation.
